import 'package:canti_hub/pages/main_page/pages/add_device_page/widgets/device_type_widget.dart';
import 'package:flutter/material.dart';

class AddBluetoothDeviceWidgets extends StatelessWidget {
  const AddBluetoothDeviceWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Add other Bluetooth device widgets here
      ],
    );
  }
}
